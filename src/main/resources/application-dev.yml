spring:
  datasource:
    url: ${DB_URL:jdbc:mariadb://localhost:3306/documents}
    username: ${DB_USERNAME:root}
    password: ${DB_PASSWORD:password}
  mail:
    host: ${MAIL_HOST:localhost}
    port: ${MAIL_PORT:1025}
    username: ${MAIL_USERNAME:}
    password: ${MAIL_PASSWORD:}
