<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.team2.documents.command.domain.repository.CommercialInvoiceRepository">

    <insert id="createFromPurchaseOrder">
        INSERT INTO commercial_invoices (
            ci_code, po_id, ci_invoice_date, client_id, currency_id, ci_total_amount, ci_status
        )
        SELECT
            #{ciId},
            po_id,
            CURRENT_DATE,
            client_id,
            currency_id,
            po_total_amount,
            '발행대기'
        FROM purchase_orders
        WHERE po_code = #{poId}
    </insert>

</mapper>
