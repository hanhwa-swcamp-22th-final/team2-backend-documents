<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.team2.documents.repository.ShipmentOrderRepository">

    <insert id="createFromPurchaseOrder">
        INSERT INTO shipment_orders (shipment_order_id, po_id, shipment_issue_date, client_id)
        SELECT
            CONCAT('SH', REPLACE(po_id, 'PO', '')),
            po_id,
            CURDATE(),
            client_id
        FROM purchase_orders
        WHERE po_id = #{poId}
    </insert>

</mapper>
